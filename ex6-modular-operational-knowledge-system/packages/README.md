# EX6 First-Party Packages

This directory is for first-party package source trees.

Current state:

- the runtime foundation is implemented
- the package source directories are now laid out
- the package business logic is still mostly ahead of us

The initial first-party package split is:

- `context/`
- `knowledge/`
- `runs/`
- `links/`
- `procedures/`

Source: `DI-moksu`.
