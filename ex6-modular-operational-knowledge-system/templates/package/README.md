# EX6 Package Template

Copy this directory to start a new installed executable package.

After copying:

1. rename the package ID
2. rename the family and protocol IDs
3. rename the command path
4. update the claim block
5. implement your real `validate` and `run` behavior

Then install with:

```bash
moks package install ./your-package
```
