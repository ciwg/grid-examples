# Knowledge Package

Planned ownership:

- knowledge items
- revisions
- lifecycle transitions

This package should carry the modular replacement for the central ex5 document
model.

Current state: first built-in implementation now exists in `package.go`, with
commands for item creation, listing, inspection, revision snapshots, and
lifecycle changes. Source: `DI-vakod`.
