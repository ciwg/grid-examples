# Maintenance Package

Planned ownership:

- maintenance item kind
- maintenance service session records
- maintenance finding records

This package should carry the first dedicated maintenance domain behavior above
shared `knowledge`, `runs`, and `context` families.

Current state: first built-in implementation now exists in `package.go`, with
commands for maintenance creation, listing, inspection, service recording, and
finding recording. Source: `DI-ramek`.
