# Training Package

Planned ownership:

- training item kind
- training session records
- training completion and certification records

This package should carry the first dedicated training domain behavior above
shared `knowledge` and `runs` families.

Current state: first built-in implementation now exists in `package.go`, with
commands for training creation, listing, inspection, session recording, and
completion recording. Source: `DI-sivuk`.
