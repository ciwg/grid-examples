# Receiving Package

Planned ownership:

- receiving item kind
- receipt session records
- receiving disposition records

This package should carry the first dedicated receiving domain behavior above
shared `knowledge`, `runs`, and `context` families.

Current state: first built-in implementation now exists in `package.go`, with
commands for receiving creation, listing, inspection, receipt recording, and
disposition recording. Source: `DI-zibek`.
