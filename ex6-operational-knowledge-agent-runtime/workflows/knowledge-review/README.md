# Knowledge Review Workflow

This loadable workflow records an explicit approval for an existing reviewed
knowledge item.

It composes `knowledge`. When its exact artifact is active in the workflow
local runtime, its trusted built-in adapter executes the declared knowledge approval
command. Source: DI-lumek.

Source: DI-dovuk.
