# external.note.v1

Status: frozen test fixture

## Record contract

This test-only external family uses the canonical Grid package-record carriage. Its payload is canonical JSON whose test scenarios determine the asserted fields. It exists solely to prove that an explicitly identified external family is rejected or preserved according to the kernel's local validation path without synthesizing a pCID.

## Evolution

This fixture is immutable. A changed test contract requires a new fixture specification and CID. Source: DI-solan.
