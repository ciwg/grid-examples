# knowledge-link

`knowledge-link` is the family for typed links across durable records.

Current ex5 implementation intent:

- connect responsibilities to items
- connect runs to other operational records
- preserve typed relation names instead of untyped references only

The current code models this family through `Link`.
