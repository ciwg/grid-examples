# knowledge-evidence

`knowledge-evidence` is the family for structured evidence attached to
performed work.

Current ex5 implementation intent:

- summary text
- structured facts
- optional copied attachment
- actor and timestamp

The current code models this family through `Evidence` records attached to
`RunRecord`.
